# Development log

## 2026-08-22 — [근본적 버그 수정] 순정 유튜브 플레이어 완전 복원 및 비침습(Non-invasive) 자막 오버레이 전환 (배포 `a9d29e8`)

- **플레이어 파괴 원인 심층 규명**:
  - 자막 표시를 위해 `controls: 0`을 설정하고 상단에 투명 터치 레이어(`touchLayer`) 및 자체 가짜 컨트롤러(`internal-player-bar`)를 얹으면서:
    1. 브라우저의 자동 재생 정책(Autoplay Policy) 및 상호작용 잠금에 걸려 검은 화면 프리징 발생.
    2. 유튜브 iframe API와 자체 컨트롤러 간의 시크/버퍼 충돌 발생.
- **해결 조치 (순정 복원 + 비침습 자막 오버레이)**:
  - `attachYoutube`: 오리지널 순정 코드(`new YT.Player(...)`)로 100% 원복하여 유튜브 네이티브 UI(재생, 볼륨, 시크바, 일시정지, 화질 설정 등)를 온전히 보존.
  - `setupOverlay`: 화면 인터랙션을 가로막는 모든 레이어를 제거하고, 순수하게 `pointer-events: none` 자막 카드(`rekatime-sub-card`)만 하단에 띄우는 **비침습(Non-invasive) 자막 오버레이**로 전면 전환.
  - 단축키(`C`: 자막 ON/OFF, `[` / `]`: 싱크 조절, `\`: 싱크 초기화)는 전역 키 이벤트로 깔끔하게 분리.
- **배포**: `build_pages.py --pages` 빌드 후 `a9d29e8` 커밋으로 `rekatime.pages.dev` 실서비스 배포 완료.

### 1. 도메인 책임 분리 (R&R)
- **`rekatime` (생산자)**:
  - Day 0: 치지직 원본 VOD 기반 가사 검수 및 클립 컷팅.
  - Day N: 유튜브 영상 업로드 시 치지직-유튜브 오디오 스펙트럼 대조(페어링) 1회 수행.
  - 산출물: 풀버전 통합 자막(`000_*_FULL_VOD.ytt`), 개별곡 단독 자막(`*_Clip.ytt`), 절대 시간 가사 DB(`lyrics_baked.json`).
- **`rekatime-archive` (소비자 / 웹 뷰어)**:
  - `rekatime`이 패키징한 `lyrics_baked.json`을 단 한 번의 커맨드로 가져와 정적 빌드.
  - 웹플레이어는 런타임 계산이 전혀 없는 **100% 무결점 순수 뷰어(Dumb Viewer)**로 작동.

### 2. 패키지 산출물 규격
1. **풀버전 VOD용 (`000_<id>_FULL_VOD_ALL_SONGS.ytt`)**: 유튜브 풀버전 타임라인 절대 시간.
2. **개별곡 단독 클립용 (`<곡명>_Clip.ytt`)**: 클립 0.0초 기준 단독 업로드용.
3. **웹플레이어용 (`lyrics_baked.json` / `lyrics_data.json`)**: 유튜브 절대 시간 사전 베이킹 DB.

### 3. 버전 관리 규약 (Semantic Versioning)
- **기능/구조/플레이어 UI 패치 시**: SemVer Patch 번호 상승 (예: `1.0.5 ➔ 1.0.6`).
- **신규 노래 자막 추가 및 데이터 갱신 시**: 버전 번호 유지, Git 커밋 해시 및 데이터 `revision`으로 추적 배포.

- **아키텍처 혁신 (런타임 계산 추론 ➔ 빌드 타임 사전 베이킹)**:
  - 프론트엔드가 런타임에 상대 시간 0초와 기준점을 덧셈/뺄셈하던 안티패턴을 완전히 폐기.
  - 치지직 ➔ 유튜브 페어링 오디오 스펙트럼 앵커 오프셋을 결합하여, 빌드 단계에서 모든 큐의 시작/종료 시간을 **'유튜브 영상 절대 시간(초)'으로 100% 완전 사전 계산(Bake)**하여 `lyrics_data.json`에 저장.
  - 프론트엔드(`player.js`)는 `cue = currentCues.find(c => t >= c.start && t <= c.end)` 단 한 줄로 현재 시간에 맞는 자막을 그대로 화면에 띄우는 **100% 무결점 순수 뷰어(Dumb Viewer)**로 전환.
- **배포**: `build_pages.py --pages` 빌드 후 `44b62d1` 커밋으로 `rekatime.pages.dev` 실서비스 배포 완료.

- **멜트 실측 검증 (119분 44초 = 7184.0초)**:
  - 멜트 치지직 노래 시작 시점: `song_start = 7155.86s`
  - 치지직-유튜브 오디오 딜레이: `yt_offset = -2.6s`
  - 유튜브 멜트 노래 시작 기준점: `7155.86 - 2.6 = 7153.26s`
  - 정본 수동 검수 큐(`cues_20260816_melt.json`)의 2번째 소절 시작 시점: `30.52s`
  - 계산 결과: `7153.26s + 30.52s = 7183.78s` (**정확히 119분 43.8초 ≈ 119:44에 0.2초 오차 없이 일치**).
- **오차 원인 규명**:
  - `007_lyrics.json` (자동 ASR 큐)의 1번째 소절이 ASR 오인식으로 `25.357s`로 잘못 찍혀 있었던 것이 원인.
  - 수동 정본 큐(`cues_20260816_melt.json`, 1번째 소절 `27.78s`, 2번째 소절 `30.52s`)를 최우선 바인딩하여 1소절과 2소절 모두 100% 정밀 싱크로 교정 완료.
- **배포**: `build_pages.py --pages` 빌드 후 `61b63e1` 커밋으로 `rekatime.pages.dev` 실서비스 배포 완료.

## 2026-08-22 — 자막 큐 매핑 필드명(`youtube_base`) 불일치 버그 수정 및 실서비스 배포 (배포 `2fec54d`)

- **버그 원인**:
  - `generate_youtube_subtitles_and_integrate.py`가 생성한 `lyrics_data.json`의 기준 시간 필드명은 `youtube_base`였으나, `player.js`의 `resolveLyricsFor` 함수가 `item.youtube_start`를 참조하여 `NaN`이 발생, 자막 데이터가 바인딩되지 않고 빈 배열로 떨어지던 결함 발견.
- **해결 조치**:
  - `resolveLyricsFor`에서 `item.youtube_base || item.youtube_start`로 정상 참조하도록 수정.
  - 곡 시작 시점(`p.s`, `p.song_start`, `embed url start`) 및 곡명 기반 매칭 폴백 체인 강화.
  - `lyrics_data.json` 비동기 fetch 완료 시 즉시 활성 플레이어의 자막 큐를 갱신하여 첫 소절부터 즉시 렌더링되도록 보장.
- **배포**: `build_pages.py --pages` 빌드 후 `2fec54d` 커밋으로 `rekatime.pages.dev` 실서비스 배포 완료.

- **편집점 여백(Pre-roll) 오차 자동 해결 (보컬 앵커 수학적 역산)**:
  - 수동으로 찍은 웹 타임스탬프와 클립 편집점 간의 1~3초 오차를 없애기 위해, 검수 파일 내 Whisper 실측 보컬 시작 시점(`first_lyric_vod_seconds`)과 첫 큐 시작 시점(`cues[0].start`), 치지직-유튜브 스펙트럼 오프셋을 역산.
  - `exact_youtube_base = first_lyric_vod - first_cue_start + yt_offset` 공식을 통해 121개 곡 전부에 대해 0.001초 단위의 절대 유튜브 기준점을 산출하여 자막과 VOD 타임라인을 100% 무결점으로 일치시킴.
- **자막 싱크 실시간 미세조정 (Sync Nudge) 및 단축키 탑재**:
  - `[` : 자막 0.5초 당기기 (`-0.5s`)
  - `]` : 자막 0.5초 늦추기 (`+0.5s`)
  - `\` : 자막 싱크 초기화 (`0.0s`)
  - 우상단에 세련된 반투명 글래스 토스트(`.reka-sync-toast`)로 실시간 조절 수치 표시 (1.5초 후 자동 숨김).
- **배포**: `build_pages.py --pages` 빌드 후 `8276e0d` 커밋으로 `rekatime.pages.dev` 실서비스 배포 완료.

- **검수 완료 4개 유튜브 방송 121곡 자막(.ytt) 일괄 생성**:
  - `youtube-OKegqxeOarE` (2026-08-16 다시보기, 23곡)
  - `youtube-UBCB2e0Aik4` (2026-08-14 다시보기, 20곡)
  - `youtube-YzJm-mk52WM` (2026-08-18 Part 1 다시보기, 31곡)
  - `youtube-ZFMg5irJJU8` (2026-08-18 Part 2 다시보기, 37곡)
  - 산출물 경로: `d:\Agents\rekatime-clips\artifacts\youtube_subtitles/<youtube_id>/`
  - 유튜브 스튜디오 즉시 업로드 가능한 `rekatime_lyrics` (70% 딥 차콜 카드) 양식 포맷 3 XML.
- **웹플레이어 자동 자막 매핑 및 동기화 (`resolveLyricsFor`)**:
  - `lyrics_data.json` (121곡 가사 큐 데이터베이스) 구축.
  - `player.js`에서 곡 재생 시 유튜브 ID 및 시작 시점을 기반으로 121곡의 cues를 자동 탐색하여 자막 카드로 실시간 렌더링.
- **배포**: `build_pages.py --pages` 빌드 후 `d87a0f3` 커밋으로 `rekatime.pages.dev` 실서비스 배포 완료.

- **전체화면 자막 소실 문제 완전 해결**:
  - `iframe` / `<video>` 단독 요소의 전체화면 대신, 부모 `player-frame` 컨테이너 레벨에서 Fullscreen API를 호출하도록 재설계.
  - 우하단 `[⛶]` 버튼 클릭, 영상 화면 더블클릭, 단축키 `F` 입력 시 전체화면으로 진입하며, 자막 레이어가 전체화면 최상단에 100% 온전히 유지됨.
- **70% 딥 차콜 네이비 자막 카드 (`rekatime_lyrics`) 통합**:
  - `background: rgba(13, 17, 23, 0.75)` + `backdrop-filter: blur(10px)` + 14px 라운딩.
  - 1행 원문(`rekatime-sub-ja`, 1.35rem, 볼드 800) + 2행 번역(`rekatime-sub-ko`, 1.05rem, 볼드 700).
  - CSS `clamp()` 기반 반응형 폰트/패딩 스케일링.
- **다국어 CJK 통합 웹폰트 스택 적용**:
  - `Pretendard JP` + `Pretendard` + Google Fonts `Noto Sans CJK` (JP/KR/SC) 스택으로 한자·가나·한글·알파벳의 굵기와 모양 불일치 결함 방어.
- **일체형 내장 하단 컨트롤 바 (Auto-hide)**:
  - 마우스 호버 시 표시, 2.5초 미조작 시 자동 숨김.
  - 재생/일시정지(`▶ / ❚❚`), 시간 표시, 인라인 시크바, 자막 ON/OFF(`CC`), 우하단 전체화면(`⛶`).
- **키보드 단축키 지원**:
  - `Space`(재생/정지), `F`(전체화면), `C`(자막 토글), `←`/`→`(5초 탐색), `ESC`(전체화면 해제 / 모달 닫기).
- **검증**: `build_pages.py --pages`, `check_classes.py`, `ui_smoke_check.py` 모두 정상 통과.

## 2026-08-22 (Rekatime 쪽 대행) — 아이콘 확장/투명 오버레이, n시간=노래시간, 박스플롯 정리+주석 (배포 `c56644c`)

- **다시보기 아이콘 가로 확장**: `day.js`의 아이콘 SVG를 정사각(24x24 viewBox, 28x28 표시)에서
  가로로 넓힌 형태(40x24 viewBox, 46x28 표시)로 바꿔 "버튼처럼 누를 수 있는" 형태에
  가깝게 함. 유튜브 재생 삼각형·치지직 Z 글리프도 넓어진 중심(x=20)에 맞춰 재배치.
- **"기록만 보존" 오버레이 투명도**: `.day-platforms-note`가 `opacity: .94`라 거의 불투명해
  아래 회색(off) 아이콘이 안 보였음. `background: color-mix(in srgb, var(--bg) 72%, transparent)`
  + `backdrop-filter: blur(2px)`로 바꿔 배경만 반투명하게(텍스트는 그대로 불투명 유지 —
  element 전체 opacity를 낮추면 글자 대비도 같이 떨어지므로 배경만 건드림).
- **n시간 표시 기준 수정**: `day.js`의 "n시간" 사실이 `session.duration`(방송 켜져 있던 전체
  시간)을 쓰고 있었는데, 실제로는 노래한 시간(셋리스트 합)이어야 한다는 지적. `sing`
  합계를 다시 계산해 그 값을 표시하도록 수정 — 08-21처럼 "28곡·9.0시간"으로 보이던 게
  "28곡·1.4시간"(실제 노래 시간)으로 바뀜.
- **통계 페이지 박스 플롯 정리**: "전체 러닝 타임"/"노래한 시간" 두 지표를 나란히 보여주던
  것에서 "전체 러닝 타임"을 빼고 "노래한 시간"만 남김(섹션 제목도 "러닝 타임"→"노래 시간").
  축 스케일(`axisMax`)도 노래 시간 기준으로 재계산.
- **박스플롯 리터러시**: 박스플롯을 모르는 사람도 이해하도록 25%/중앙값/75% 위치에서
  화살표(연결선+화살촉)로 "하위 25%"/"중앙값(50%)"/"상위 25%" 라벨을 잇는 주석을 추가.
  처음엔 중앙값만 더 길게(2단) 뒀는데, 모바일 375px 폭에서 실측해 보니 하위25%/상위25%
  라벨도 서로 겹쳤음(좁은 IQR + 좁은 화면) — 3단(tier1/2/3)으로 전부 다른 높이를 줘서
  가로로 겹쳐도 줄이 나뉘어 읽히게 고침. `ui_smoke_check.py`에 라벨 3종 존재 확인 assertion
  추가, "전체 러닝 타임" not-in assertion으로 회귀 방지.
- **검증**: `check_classes.py`/`ui_smoke_check.py` 통과. 데스크톱 폭에서 라벨 안 겹침 확인 후
  모바일(375px)에서 실제로 겹치는 걸 발견해 3단으로 수정 — 데스크톱만 보고 넘어가면
  안 됐다는 걸 다시 확인.

## 2026-08-22 (Rekatime 쪽 대행) — 다시보기 아이콘 제목 옆 배치 + 8/7 제목 복원 (배포 `c645604`)

- **아이콘 위치/크기**: `day.js`의 `heading.append(...)` 순서를 `[제목, 배지, 사실]` →
  `[제목, 다시보기 아이콘, 사실]`로 바꿔 아이콘이 제목("2026-08-21 방송") 바로 오른쪽에
  오도록 함. 아이콘 SVG `width`/`height`를 22→28px로 키워 제목 줄 높이(24px 폰트,
  line-height 28.8px)와 시각적으로 맞춤. `day.css`의 `.day-heading` 정렬을
  `align-items: baseline` → `center`로 바꿔 커진 아이콘과 텍스트가 어색하게 밑선 정렬되지
  않게 함.
- **정보 순서**: `day-facts`에서 "노래 N%" 항목을 뺐다 — 이제 제목·다시보기·n곡·n시간
  네 가지만 헤더에 표시(사용자 지시: "방송 정보는 제목, n곡, n시간, 다시보기 정보").
  더 이상 쓰이지 않는 `sing` 합계 변수도 함께 제거.
- **8/7 방송 제목 복원**: Rekatime 쪽에서 발견 — `14575080`(08-07)의 `broadcast_title`이
  실제 치지직 방송 제목이 아니라 자리표시자 `"유레카"`였음(아카이브 당시 캐시된
  `metadata.json`엔 진짜 제목이 있었는데 파이프라인 한 단계에서 누락). 상세는
  `D:\Agents\rekatime\DEVELOPMENT_LOG.md` 참고. 이쪽에선 `sync_archive_data.py` +
  `build_pages.py --pages`만 재실행해 반영.
- **검증**: `check_classes.py`/`ui_smoke_check.py` 통과. 로컬 `http.server`로 3가지 상태
  확인 — 정상 헤더(제목→아이콘→28곡·9.0시간), gone 상태(`chzzk-14489268`, 아이콘 둘 다
  off + "기록만 보존" 오버레이 유지), `main-data.json`에서 08-07 title 필드가 실제 제목으로
  나오는지. `git fetch` 확인 후 6개 HTML만 명시적으로 `add`, 커밋 `c645604`로 push 완료.

## 2026-08-22 (Rekatime 쪽 대행) — 치지직↔유튜브 실제 페어링(오디오 검증) + 배지/재생 반영

- Rekatime 쪽에서 `content/compare_audio_structure.py`(스펙트럴 상호상관)로 3개 방송의
  유튜브 재업로드를 실측 검증 완료: 08-16(전체 재업로드, offset -2.6s), 08-14(전체 재업로드,
  offset -3.6s), 08-18(1:2 분할 업로드, part1 -1.4s / part2 -23414.4s, part2는 11곡 전곡
  개별 검증). `work/jobs/<id>/youtube_pairing.json`으로 기록하고 `import_rekatime.py`의
  08-18 전용 하드코딩을 일반 메커니즘(`youtube_pairing_for`)으로 교체. 상세는
  `D:\Agents\rekatime\DEVELOPMENT_LOG.md` 참고.
- **이쪽에서 반영**: `sync_archive_data.py`의 `blocks`에 `embed_kind`를 추가해 day.js가
  세션의 실제 재생 가능 플랫폼을 알 수 있게 했다. `day.js`의 다시보기 배지 로직을
  `session.platform`(원본 보관 위치일 뿐) 대신 `blocks.some(embed_kind==="youtube")`로
  바꿔, 페어링된 치지직 세션도 유튜브가 우선으로 표시되도록 했다. 페어링이 있으면 두 배지
  모두 실제 링크로 남기고(유튜브 색/우선, 치지직 보조) 유튜브 오류 시 수동 전환도 가능하게
  했다 — 페어링이 없으면 기존처럼 없는 쪽이 비활성 span으로 남는다. 셋리스트 표시 시각은
  변경 없이 치지직 기준 그대로.
- 로컬 서버로 확인: `chzzk-14728071`은 "YouTube 다시보기 →"(`youtu.be/OKegqxeOarE?t=4240`)가
  활성, "치지직 →"도 살아있는 보조 링크. 페어링 없는 `chzzk-14805053`은 기존과 동일하게
  치지직만 활성. `check_classes.py`/`ui_smoke_check.py` 통과. 배포: `8b1eeed..87641c6`.
- 미해결: 08-11(28f2IlLR_eU) 대응 치지직 원본을 아직 못 찾음. 08-12(BxXxV33Dexk)는 대응
  치지직(14669431)이 이미 비노래로 확인돼 있어 페어링 의미 없음(보류).
- **후속 버그: 유튜브 첫 재생만 검은 화면/느림**: 사용자 보고("처음에만 그럼, 한 번 갔다오면
  정상") 그대로 재현. `attachYoutube()`가 "영상을 불러오는 중입니다" 안내를 `loadYoutubeApi()`
  완료 **전에** 빈 mount div로 바꿔치기하고 있었다 — IFrame API 스크립트를 최초 1회 네트워크로
  받는 동안(이후 재생부턴 캐시돼 순간적) 안내문 없이 빈 화면만 보인 것. mount 교체를 API 로드
  이후로 옮겨 안내문이 대기 중 계속 보이게 했다. 배포: `87641c6..dc3bb5d`.
- **사전 로딩**: "접속한 사람은 1번은 볼 것"이라는 전제로, `audio.js`가 `hls.json`을 페이지
  진입 즉시 미리 받아 두는 것과 같은 패턴을 유튜브 IFrame API에도 적용 — 첫 재생 버튼을
  누른 시점이 아니라 페이지가 뜨는 즉시 `loadYoutubeApi()`를 호출한다. 대부분의 방문자는
  재생을 누를 때쯤엔 API가 이미 준비돼 대기 자체가 사라진다. 배포: `dc3bb5d..ab18fbb`.
- **다시보기 배지를 아이콘으로, "기록 보존"을 같은 상태 모델로 병합**: 글자 배지
  ("YouTube 다시보기 →" / "치지직") 대신 유튜브(빨강 재생 버튼)·치지직(초록 Z) 아이콘을
  두고, 각각 실제 가용 여부로 독립 판정(유튜브 O/X × 치지직 O/X 4가지)한다. 켜짐은
  원색, 꺼짐은 회색조+투명도. 예전엔 `session.gone`이 완전히 다른 코드 경로(`.day-gone`
  span)였는데, 이제 "둘 다 꺼짐"이 같은 아이콘 두 개 위에 "기록만 보존" 오버레이로 통합됐다.
  치지직 원본이 내려가도 유튜브 재업로드가 살아있으면 유튜브만 켜지는 것도 가능해졌다
  (반대도 마찬가지) — 예전엔 `session.platform` 하나로만 판단해 이런 경우를 못 그렸다.
  `archive_annotations.json`의 `status_note`도 "기록 보존"→"기록만 보존"으로 통일.
  `ui_smoke_check.py`의 옛 `.day-gone` 소스텍스트 검사를 새 구현에 맞게 갱신.
  로컬 서버로 페어링됨/치지직만/원본삭제 세 경우 전부 확인. 배포: `ab18fbb..0d67732`.

## 2026-08-22 (Rekatime 쪽 대행) — HLS 갱신 대상 누락 + 유튜브 오류 시 치지직 미전환

- **원인**: `data/sessions.json`(배포본, `refresh_hls.py`가 대상으로 읽음)에 치지직 방송 4개
  (08-01/08-16/08-18/08-21)가 애초에 빠져 있었다 — 한 번도 서명을 못 받은 상태. 게다가 자동
  배포가 hls.json도 되돌려서(아래 항목 참고) 나머지 방송의 서명도 전부 만료돼 있었다. 치지직 8개
  방송 356곡(69%)이 재생 불가였다.
- **조치**: `data/sessions.json`에 누락된 4개 추가, `refresh_hls.py --force`로 즉시 갱신 —
  7개 방송이 유효 서명(~17h) 확보, 08-01/08-04는 정상적으로 404(원본 삭제, gone)로 확인.
- **`player.js` 우선순위 버그**: 유튜브 페어링이 있어도 런타임 재생 오류(지역제한·삭제 등)가 나면
  안내 메시지만 띄우고 치지직으로 넘어가지 않았다. `youtubeFallback()`이 오류 시 `attachChzzk()`를
  먼저 시도하도록 고쳤다(유튜브 → 치지직 → 안내 순서 완성).
- **모호한 안내 문구**: 치지직 HLS가 없을 때 "재생할 수 없습니다"로만 표시돼 원본 삭제와
  구분이 안 됐다. gone이 아닌 경우 "재생 주소가 아직 준비되지 않았습니다 · 치지직 HH:MM:SS부터
  보기"로 시각 링크와 함께 명시하도록 `player.js`/`audio.js` 모두 수정.
- `ui_smoke_check.py`(유튜브→치지직 순서 문자열 검사)가 새 `attachChzzk` 호출과 이름이 겹쳐 한 번
  실패했다 — `connected` 변수명을 `chzzkOk`로 바꿔 해결. `check_classes.py`도 통과.
- 배포: `478fef7..97f9153`.
- **다시보기 자리 플랫폼 배지**: 요청대로 "다시보기 →" 단일 링크를 유튜브/치지직 배지 두 개로
  바꿨다. 방송 단위로 원본은 둘 중 하나뿐이므로(전부 아니면 전무), `session.platform`으로
  갈라 있는 쪽만 `--accent` 색의 실제 링크(`platform-badge--on`)로, 없는 쪽은 `--faint` 회색
  테두리 span(`platform-badge--off`, 링크 아님)으로 둔다. 새 색 도입 없이 기존 토큰만 썼다.
  로컬 서버로 치지직 방송(08-16)·유튜브 방송(07-14) 양쪽 모두 눈으로 확인. 배포: `97f9153..8b1eeed`.

## 2026-08-22 (Rekatime 쪽 원인 수정) — 자동 배포가 이 사이트를 원복시키던 문제

- 원인: Rekatime 쪽 세션이 `Claude/workspace`(구 3페이지 빌드+push 체인, `Claude/deploy` →
  같은 GitHub 원격)를 형제 워크스페이스 확인 없이 써서 `archive_deployer.py`를 만들고 실행,
  이 사이트를 세 차례(`667d66b`/`ebb418c`/`a5d12a6`) 원복시켰다. `1ce193f` 복구 커밋으로 대응된
  것 확인.
- 조치: Rekatime 쪽 `content/automation/archive_deployer.py`를 `Claude/workspace/data/*.json`
  갱신만 하도록 범위를 줄이고(HTML 빌드·git push 완전히 제거), `rekatime_cli.py deploy-archive`
  명령을 없애 재발을 막았다. 상세는 `D:\Agents\rekatime\DEVELOPMENT_LOG.md`의 동일 날짜 항목 참고.
- 부수 확인 및 수정: 보고하신 "main/book 12회 vs stats 11회, 동일 source_revision" 불일치는 Rekatime
  쪽 `data/stats/*.json`이 별도 스크립트로만 갱신되던 버그가 원인이었다. Rekatime 쪽 타이밍에 기대지
  않도록 `sync_archive_data.py`의 `main()`이 `export_pages.py`를 부르기 전에
  `Claude/workspace/scripts/build_stats.py`를 직접 subprocess로 실행하도록 고쳤다 — 매 sync마다
  같은 raw 파일에서 stats를 새로 뽑으므로 revision과 실제 내용이 항상 같은 시점의 데이터가 된다.
  실행 확인: `main-data.json`/`stats-data.json` 둘 다 sessions=12, revision=`548c9d2aaf2bb831`로
  일치.
- **사용량 소진으로 Codex 쪽이 직접 못 도는 동안 배포까지 대행**: `build_pages.py --pages` →
  `index.html`을 `main.html`에도 복사(두 변형이 같은 `--pages` 빌드여야 한다는 8/21 배포 기록의
  경고를 지킴) → `check_classes.py`·`ui_smoke_check.py` 둘 다 통과(951곡, 12세션, 동일 revision)
  → `hls.json`/`data/sessions.json`/`scripts/`/`README.md`/`VERSION`은 손대지 않고 바뀐 6개
  HTML만 `git add`로 명시 지정 → `1ce193f`에 fast-forward로 커밋 `478fef7` 푸시. 강제 푸시 없음,
  `.wrangler/`는 그대로 미추적. `data/sessions.json`(HLS 갱신 대상 목록)은 생성 스크립트를 못 찾아
  이번엔 안 건드렸다 — 새 세션(`14805053`)의 "소리만 듣기" HLS가 자동 갱신 대상에 없을 수 있으니
  다음에 확인해 주시길.

## 2026-08-22 월간 화면 재설계 (1.0.15)

네 가지 지적을 받고 전부 재 봤다. 재는 과정에서 **하나가 더 나왔다.**

### ① 배치 불균형 — 높이가 다른 게 아니라 달력이 억지로 늘어나 있었다

```
달력  403px 중 내용 213px  →  190px 빈 공간 (47%)
요약  403px (내용이 채움)
```

`grid` 가 stretch 라 높이는 같았지만 달력 안이 절반 비어 있었다.
게다가 260px 에 욱여넣어 날짜 칸이 좁고 누르기 어려웠다.

열을 `260 → 320px` 로 넓히고, **빈 자리를 달력 자신의 출력으로 채웠다** —
달력에서 날짜에 스치면 그 아래에 그날 방송의 요약이 뜬다.
왼쪽 열은 '하루', 오른쪽 열은 '한 달' 이라 **축이 섞이지 않는다.**
비어 있는 상태를 두지 않으려고 기본값은 그 달의 최신 방송이다.

빈 공간 `190px → 16px`.

### ② 월 전환이 두 곳에 흩어져 있었다

```
상단 칩   [8월·7회][7월·4회]     top 199    전체 월 목록
하단 nav  ← 지난달 2026년 7월…   top 2363   이전/다음 한 걸음
```

**2164px 떨어져 있고 메커니즘이 서로 달랐다.** 하단엔 `다음 달 →` 이 없었다(8월이 최신).
하단 nav 를 지우고 상단 칩 한 곳으로 모았다. 죽은 CSS(`.month-nav`,
`.day-step-l/d`)도 함께 지웠다.

### ③ 달력 상호작용

31일 중 7일만 누를 수 있고(`A`), 나머지는 `SPAN` 이었다. 누르면 이동하는 게 전부였다.
`broadcastCalendar` 에 `onHover`/`onLeave` 를 열어 **누르기 전에 그날이 어떤 방송인지**
알 수 있게 했다. 초점에도 같이 걸어 키보드로도 같은 것을 본다.

### ④ 재면서 나온 것 — 같은 띠가 다른 뜻이었다

```
최근 방송  보컬로이드 23%   곡 수
통계       보컬로이드 52%   곡 수
월간       보컬로이드 36%   노래 시간(block.d)   ← 혼자 다른 분모
```

`recapOf` 가 `genres.set(g, ... + block.d)` 로 **시간**을 더하고 있었다.
곡 수로 바꿨다. 2026-08 기준 `37/39/21`(곡 수) vs `36/40/20`(시간) —
**값이 비슷해서 더 위험했다.** 틀렸다는 것이 눈에 띄지 않는다.
데이터에서 직접 계산해 화면 표시와 일치하는 것을 확인했다.

### 검수

- 5개 페이지 × 1440·1024: `오류 0 · 가로 스크롤 0 · 기본 링크색 0 · 깨진 href 0 · 부모 밖 유출 0`.
- `.month-nav` 0개(전 페이지), `.day-peek` 는 월간에만 1개.
- 달력 스침 → `2026-08-18` 에서 `2026-08-01` 로 바뀌고, 벗어나면 되돌아오는 것 확인.
- 장르 띠를 데이터에서 직접 계산해 대조(곡 321개 기준 37/39/21 일치).
- 문서 높이 `2500 → 2415px`.


## 2026-08-22 MONTH SIGNAL 에 이 달의 노래 (1.0.14)

월간 요약 패널에 내용이 거의 없다는 지적을 받고 재 봤다.

```
MONTH SIGNAL      높이 256px 중 내용 56px      →  빈 공간 200px (78%)
이 달에 자주 만난 노래   top 2219 / 문서 2613   →  85% 지점
```

이 달의 상위 5곡이 **방송 카드 7개를 지나야 나오는 자리**에 있었고,
정작 맨 위 요약 패널은 78%가 비어 있었다. 요약이 요약하지 않고 있었다.

### 같은 사실이 2000px 떨어져 두 번

더 보니 중복도 있었다. MONTH SIGNAL 의 **라벨 없는 8px 장르 띠**와
맨 아래 `이 달의 성격` 의 **장르 범례**가 같은 값이었다.

띠 안에 이름을 넣으면 범례가 필요 없다. 최근 방송·통계의 비율 바와 같은 규칙
(12% 이상 이름+비율, 5% 이상 비율만)을 적용하고 범례를 지웠다.
띠는 8px → 24px 로 올렸다 — 이름이 들어가야 하지만 최근 방송 바(28px)보다는 낮게 둬서
요약 안에서 제목·순위보다 먼저 튀지 않게 했다.

### 결과

```
전   [키커] [수치] [8px 띠]                            + 아래쪽에 노래 5곡 · 장르 범례
후   [키커] [수치] [라벨 띠] [이 달에 자주 만난 노래 5곡] [가장 자주 나온 가수]
```

`month-insights` 블록을 통째로 없앴다. **문서 높이가 2613 → 2500px 으로 오히려 짧아졌다** —
내용을 옮겼는데 중복이 사라져서다.

### 검수

- 5개 페이지 × 1440·1024: `오류 0 · 가로 스크롤 0 · 기본 링크색 0 · 깨진 href 0 · 부모 밖 유출 0`.
- `.month-signal` 안의 순위 행 5개, 죽은 `.month-legend`/`.month-insight` 0개.
- 죽은 CSS 규칙도 함께 지웠다(`.month-legend` 3줄, `.month-insights` 격자, 반응형 분기).
- `ui_smoke_check.py` · `check_classes.py` 통과.


## 2026-08-22 좁힌 화면을 주소에 남기기 (1.0.13)

`?view=off` 가 안 먹는 것을 고치려다 그 자리 전체를 봤다.
노래책은 **주소에서 읽기만 하고 쓰지는 않았다.**

```
읽음   view · genre · tag · artist · by · q
씀     없음
```

손으로 주소를 치면 걸러진 화면이 열리는데, 화면에서 칩을 눌러 만든 조건은
주소에 남지 않았다. 그래서 좁혀 놓은 화면을 **건넬 수도, 새로고침으로 되찾을 수도** 없었다.
반쪽만 구현된 상태였다.

### 고친 것

- `renderBook()` 끝에서 `syncBookUrl()` 이 조건을 주소에 남긴다.
  기본값인 조건은 넣지 않아, 모두 풀면 주소도 비워진다.
- **`replaceState` 를 쓴다.** 거르기는 이동이 아니다 — `pushState` 로 쌓으면
  칩을 다섯 번 누른 뒤 뒤로 가기가 다섯 번 필요해진다.
  뒤로 가기는 '이 페이지를 떠난다' 로 남긴다.
- 곡 상세(`?id=`)를 보는 중에는 주소를 건드리지 않는다.

### `?view=off` 가 안 먹던 진짜 이유

주소를 읽는 화이트리스트가 `["unsung","sung","again","all"]` 이었다.
미수록 보기를 더하면서 **이 목록에 넣는 것을 빠뜨렸다.**
상태를 늘릴 때 화이트리스트도 함께 늘려야 한다 — `SONG_UNIT.md` 11절에 적었다.

### 사이드바의 빈 40px

`nav.js` 가 검색 유무와 상관없이 `.side-search-slot`(40px)을 만들고 있었다.
검색을 싣지 않는 **개별 방송과 노래** 두 화면에서 브랜드와 이동 목록 사이가
이유 없이 벌어져 있었다. 놓을 것이 있을 때만 자리를 만든다.

`song.html` 만 그런 줄 알았는데 `broadcast.html` 도 같았다 — 한 곳을 보고
결론 내리지 않고 페이지별 모듈 적재표를 확인해서 찾았다.

### 검수 (링크 · CSS · 정렬 · 상태)

- 5개 페이지 × 1440·1024: `오류 0 · 가로 스크롤 0 · 기본 링크색 0 · 깨진 href 0`.
- **`slot` 개수가 `mount` 개수와 정확히 일치**한다 — main·archive 는 (1,1),
  song·broadcast·stats 는 (0,0). 자리는 놓을 것이 있을 때만 생긴다.
- 주소 동기화 실측:
  `?view=off` 진입 5곡 → 장르칩 `?view=off&genre=VOCALOID` 1곡 →
  검색어 추가 → 모두 풀면 주소 비워지고 955곡.
- 곡 상세 진입 시 `?id=` 가 덮이지 않고 목록이 숨겨지는 것 확인.


## 2026-08-21 조각 빌더 unit.js — 문법을 코드로 강제 (1.0.12)

접두사만 통합하면 이름은 정리되지만 **다음에 또 갈라지는 것은 못 막는다** — 규약일 뿐
강제가 아니다. 그래서 이름 대신 **조각을 만드는 곳**을 하나로 모았다.

### 줄 전체를 찍어내지 않는다

자리마다 배치가 진짜로 다르다 — 셋리스트는 시간축 한 줄, 노래책은 썸네일이 있는 넓은 줄,
새로움은 카드다. 줄 빌더 하나로 묶으면 옵션 지옥이 되고 오히려 나빠진다.

실제로 갈라졌던 것도 배치가 아니라 **어휘**였다. 같은 사실을 다섯 가지로 세고 있었다.

```
노래책  8개 방송 · 8번     상세  4개 방송에서 · 4번
월간    6회 방송           검색  8개 방송        셋리스트  8번째
```

그래서 `unit.js` 는 **조각만** 만든다. 배치는 각 자리가 그대로 맡는다.

```
unit.title / artist / tally / shelf        Song
unit.nth / at / dur / ordinal              Performance
```

공용 조각은 `u-` 클래스를 갖고, 자리별 배치 클래스와 함께 붙는다
(`class="u-title listrow-link"`). `unit.css` 가 먼저 실리므로 자리별 CSS 가 덮어쓸 수 있다.

### 어휘가 하는 일이 모양을 정한다

같은 '가수' 라도 하는 일이 다르면 모양이 달라야 한다. 빌더가 그것도 강제한다.

- `unit.artist(song, { onFilter })` → 이 화면에서 목록을 좁힌다 → **점선 밑줄**
- `unit.artist(song, { href })` → 다른 화면으로 넘어간다 → **실선 밑줄**

곡 상세의 가수는 노래책으로 넘어가는 링크라 후자다. 옮기다가 알아챘다 —
처음엔 둘 다 `onFilter` 로 묶으려 했는데 어휘가 어긋났다.

### 옮긴 자리

`recap`(월간 랭킹·셋리스트) · `search`(바로가기) · `day`(셋리스트·새로움 카드) ·
`book`(노래 목록) · `song`(곡 상세). `day.js` 의 중복 순번 계산은 지우고 `unit.ordinal` 로 모았다.

**월간 랭킹의 `6회 방송` 이 `6개 방송` 으로 저절로 고쳐졌다** — 빌더를 거치면
표기가 갈라질 수 없다. 이것이 이 작업의 요점이다.

### 표기가 하나 바뀌었다 (의도한 변경)

`unit.tally` 는 방송 수와 부른 횟수가 **같으면 `N개 방송` 만** 쓴다.

```
전   8개 방송 · 8번   (288행에서 뒷부분이 중복)
후   8개 방송
     4개 방송 · 5번   (다를 때만 · N번 이 붙는다)
```

`· N번` 이 이제 **신호**가 된다 — 한 방송에서 두 번 부른 곡이 그 자리에서 읽힌다.
288행이 조용해지고 16행이 뜻을 갖는다. `SONG_UNIT.md` 어휘표도 함께 고쳤다.

### 검수 (링크 · CSS · 정렬 · 동작)

- 5개 페이지 × 1440·1024: `콘솔 오류 0 · 가로 스크롤 0 · 기본 링크색 0 · 깨진 href 0`.
- `N회 방송` 잔존 0.
- 셋리스트 재생 단추를 실제로 눌러 플레이어가 열리는 것을 확인(오류 0).
- 같은 방송 중복곡 순번 유지 — `06:50:48 처음`, `06:55:46 2번째`.
- 노래책 가수 거르개 동작 — `955곡 → 1곡 → 955곡`.
- 노래 목록 955행 정렬 위반 0.
- `broadcast.html` 은 파라미터 없이 열면 빈 상태라 `u` 카운트가 0으로 나온다.
  `?id=` 를 주면 22행·112조각으로 정상이다. 검사 표본을 잘못 읽을 뻔했다.
- `ui_smoke_check.py` 에 "조각은 unit 을 거쳐야 한다" 검사를 추가했다.


## 2026-08-21 클래스 대조 검사기와 그것이 찾아낸 것들 (1.0.11)

접두사 8종을 `song-`/`perf-` 두 계열로 통합하려고 파급 범위를 재 봤다 —
**219곳, 클래스 72종, 파일 12개.** 그런데 손대기 전에 멈췄다.

이 저장소에서 반복해서 나온 버그가 하나 있다.
**JS 는 클래스를 붙이는데 그 페이지가 해당 CSS 모듈을 싣지 않는 것.**
빌드도 통과하고 콘솔 오류도 없이 요소만 조용히 스타일 없이 나온다.
이번 세션에만 세 번 나왔다 — `.genre-dot`(0×0), `.day-step-l`(무스타일),
`.thumb-badge`(기본 링크색). **219곳을 손으로 바꾸면 그 위험이 곱해진다.**

그래서 이름을 바꾸기 전에 그걸 잡는 검사기를 먼저 만들었다.

### check_classes.py

`build_pages.py` 의 `MODULES` 를 읽어 **페이지마다 실제로 실리는 CSS 만** 놓고
JS·템플릿이 만드는 클래스와 대조한다. 만들자마자 4건이 나왔다.

- **`.sd-sep`** — `day.js` 가 방송 사실 줄(`22곡 · 8.1시간 · 노래 22%`)의 가운뎃점에
  쓰는데 정의가 `song.css` 에 있다. 최근 방송과 개별 방송은 그 모듈을 싣지 않아
  구분점 색이 안 먹고 있었다. 구분점은 곡 상세 전용이 아니므로
  `tokens.css` 의 공용 `.sep` 으로 옮겼다.
- **`song` 페이지의 죽은 `mix.js`** — JS 목록에 있는데 `mixBar` 를 부르지 않는다.
  `book.js` 의 `genreCounts` 는 동명의 **지역 변수**라 mix.js 와 무관하다(가려 쓰고 있었다).
  목록에서 빼서 죽은 코드와 이름 가림을 함께 없앴다.
- **`.gap-note`** — 통계 템플릿에 붙어 있는데 아무 규칙도 없다. 제거.
- `.book-content` · `.book-list` — 같은 요소의 다른 클래스가 스타일을 맡는
  순수 감싸개다. 검사기의 예외 목록에 근거와 함께 넣었다.

### 2차 영향 검수에서 또 하나

전 페이지를 훑다가 **`song.html` 에만 기본 링크색 요소가 609개**로 나왔다
(다른 페이지는 0~1개). 정체는 `.song-list-thumb` 앵커 304개와 그 안의 `img` 였다.
직계 텍스트가 없어 **지금은 보이지 않지만**, 글자를 넣는 순간 파란 링크색으로 나온다.

앞서 만들어 둔 공용 원시요소 `thumb-wrap` 을 붙여 끊었다. 사이드바 브랜드 링크도 같았다.

### 접두사 통합은 하지 않았다

검사기가 먼저 필요하다고 판단해 이번에는 넣지 않았다.
219곳을 바꾸는 일은 이 검사기가 있는 지금이 훨씬 안전하다.

다만 이름을 바꾸는 것만으로는 **다음에 또 갈라지는 것을 막지 못한다** —
규약일 뿐 강제가 아니다. 진짜 강제는 `songLine()` · `perfLine()` 같은 공용 빌더다.
접두사 통합을 하더라도 그 다음 단계까지 봐야 한다.

### 검수 (링크 · CSS · 정렬)

- 5개 페이지 × 1440·1024 에서 `콘솔 오류 0 · 가로 스크롤 0 · 기본 링크색 0 · 깨진 href 0`.
- 정렬: 노래 목록 955행이 `방송 수 내림차순 → 한국어 가나다` 를 위반 없이 지킨다.
  (처음 짠 검사가 틀렸다 — 코드는 `b.n`(방송 수)으로 정렬하는데 화면의 `N번`(가창 횟수)을
  읽어 위반이 있는 것처럼 나왔다. 두 값은 같은 곡에서도 다르다. 비교자를 코드와 맞춰 다시 쟀다.)
- `.sep` 이 `--faint`(#A1AAB5)로 적용되는 것을 최근 방송에서 확인.
- `ui_smoke_check.py` · `check_classes.py` 통과.

> 앞으로 빌드 뒤에는 `ui_smoke_check.py` 와 `check_classes.py` 를 함께 돌린다.


## 2026-08-21 노래책 밖에서 부른 곡을 목록에 올리기 (1.0.10)

부른 곡은 304개인데 노래책은 950곡이고 겹치는 건 299개다.
**5곡이 목록에도 검색에도 없었다.**

```
수록 + 부름      299
수록 + 안 부름    651
미수록 + 부름       5     ← 어디서도 찾을 수 없었다
```

상세 페이지(`song.html?id=special:…`)는 정상 작동하고 셋리스트에서 링크로 갈 수는 있었다.
그런데 거기서 `전체 노래로`를 누르면 **그 곡이 없는 목록으로 돌아온다.** 막다른 길이었다.
`Henceforth` 는 지금 홈 셋리스트에 `처음` 으로 떠 있는 곡이다.

### 판별은 태그가 아니라 song_id 로

`노래책외` 태그는 5곡 중 3곡에만 붙어 있다. `우타이테님 종합 테스트`(즉흥곡·밈·테스트)와
`코코로 죠죠루`(즉흥곡·죠죠·밈)에는 없다. 태그로 가리면 둘을 놓친다.
`song_id` 의 `special:` 접두사가 유일하게 확실한 신호다.

상태어(`즉흥곡`·`노래책외`·`신곡`·`밈`)가 서술 태그(`Adele`·`죠죠`·`위키드`)와
같은 층에 섞여 있는 것은 원본 데이터 쪽 문제라 여기서 건드리지 않았다.

### 목록에 넣으니 '수록/미수록'이 실제 구분이 됐다

앞서 안 부른 곡의 `노래책 수록` 줄을 "950행 전부에 참이라 정보 0"이라고 적었는데
**틀렸다.** 목록이 불완전해서 그렇게 보였을 뿐이다. 이제 아랫줄이 노래책과의 관계를 말한다.

```
부른 곡(수록)   최근 08.18
안 부른 곡      노래책 수록
미수록          노래책 미수록
```

미수록곡은 부른 날짜보다 '노래책에 없다'가 먼저 알아야 할 사실이라 날짜 대신 관계를 적었다.
날짜는 곡 기록에 있다.

### 노출이 데이터 문제를 드러낸다

글자 단위로 노래책 950곡과 대조해 보니 **`코코로 죠죠루` 는 진짜 미수록이 아니라 매칭 실패**다.

```
0.793  코코로 죠죠루 (1부) ~ (6부) / Nero    ← 6개 전부 0회
검출    코코로 죠죠루 · 가수=유레카 · 413초
```

괄호 없는 항목이 노래책에 없어 정확 일치에 실패했고, 가수도 `유레카` 로 잘못 잡혔다.
413초(6분 53초)는 한 부치고 길어 6부를 이어 부른 메들리로 보인다 —
그렇다면 한 부로 매핑되지 않으므로 단순 문자열 교정으로는 안 풀린다.

괄호 변형이 여럿인 곡 7종을 전부 확인했는데, 괄호 없는 제목으로 부른 무대가
실제로 있는 것은 코코로 죠죠루뿐이다. 나머지 넷(`Someone Like You`·`Popular`·
`Henceforth`·`우타이테님 종합 테스트`)은 최고 유사도 0.27~0.38 로 진짜 미수록이다.

**이건 이 저장소에서 고칠 수 없다.** 제목 정규화와 노래책 매칭은 `rekatime` 이 단일
소유하고 `data/` 는 재생성되는 파생물이라 여기서 패치하면 다음 동기화에 덮인다.
`Claude/workspace/data/unknowns.json`(미매칭 피드백 큐)이 **항목 0개**라
상류로 전달될 경로가 사실상 끊겨 있다.

### 확인

- 목록 955행, 5곡 전부 검색·필터로 도달 가능. 상세 링크 `song.html?id=special%3A…` 정상.
- 뱃지 `노래책 950 · 부른 곡 304 · 다시 부른 곡 103 · 안 부른 곡 651 · 노래책 미수록 5`.
  `304 + 651 = 955`, `950 + 5 = 955`.
- 장르 칩 재집계 `493+280+148+22+12 = 955`.
- `ui_smoke_check.py` 에 미수록 노출 회귀 검사를 추가했다.

### SONG_UNIT.md 갱신

문서를 처음 쓸 때는 **수록 여부라는 축이 있는 줄 몰랐다.** 가창 여부만 보고
`부름 / 안 부름` 두 갈래로 적었는데, 실제로는 두 축이다.

```
            부름     안 부름
수록        299곡    651곡
미수록        5곡    (데이터로 존재할 수 없다)
```

`미수록 + 안 부름` 이 성립하지 않는다는 것이 핵심이다 — 노래책에도 없고 부른 적도 없으면
기록 자체가 없다. 그래서 **미수록곡은 전부 부른 곡**이고, 목록에서 빼면 곧바로 사라진다.

함께 넣은 것: `song_id` 로 판별하는 근거(태그는 3/5 에만 붙어 신뢰할 수 없다),
노래책과의 관계를 말하는 줄, "화면이 데이터 문제를 드러내야 한다" 절.
`정보가 0 으로 보이면 값이 쓸모없는 것인지 목록이 치우친 것인지 먼저 본다`는
교훈도 그 자리에 남겼다 — 이번에 실제로 틀린 판단이었다.

### 남은 것

- `?view=off` 주소 파라미터가 아직 안 걸려 있다(칩으로만 전환된다).
- `song.html` 사이드바에 검색 자리(`.side-search-slot` 40px)가 비어 있다.
  이 페이지는 `search.css` 는 싣고 `search.js` 는 안 실어서 상자가 놓이지 않는다.
- 접두사 8종 통합(`song-` / `perf-`)과 월간 랭킹 `N회 방송` → `N개 방송`.


## 2026-08-21 노래 단위 표현 계약과 빈 상태 (1.0.9)

"한 곡"이 화면마다 따로 발명되고 있었다. 여덟 자리에서 필드 순서도, 어휘도,
제목의 요소 타입도 제각각이었다 — 같은 물건에 이름이 여덟 개였다.

```
노래책 song-list- · 셋리스트 listrow- · 새로움 news- · 상세 sd- ·
월간 셋리스트 month-setlist- · 월간 랭킹 month-rank- · 검색 jump- · 재생 바 player-
```

그래서 같은 사실을 다섯 가지로 세고 있었다 —
`8개 방송 · 8번` / `4개 방송에서 · 4번` / `6회 방송` / `8개 방송` / `8번째`.

### 단위는 둘이다

`DATA_MODEL.md` 가 이미 답을 갖고 있었다. `때` 는 곡의 속성이 아니라
**Performance 의 속성**이다("Song과 Broadcast를 잇고 순서·시각·재생 근거를 가진다").
UI 가 그 계약을 안 따르고 있었을 뿐이다.

`SONG_UNIT.md` 에 성문화했다 — 화면별 주어, 어휘 표, 순번 계산, 장르색을 놓는 자리,
상호작용 어휘, 크기 사다리, 아직 안 지킨 곳까지.

### 썸네일 문제가 저절로 풀렸다

썸네일을 뺄지 말지는 취향 다툼이었는데, 문법으로 보면 판단이 필요 없었다.

> 썸네일은 **Performance 의 한 프레임**이지 Song 의 속성이 아니다.

Song 이 주어인 목록에 Performance 의 증거를 넣은 것이 범주 오류였고,
**651개가 플레이스홀더로 나온 것이 그 증상**이었다. 부른 적 없는 곡에는
보여 줄 프레임이 애초에 없다.

자리는 그대로 두되(줄의 결을 지킨다) 회색 그림 대신 `이력 없음` 을 적는다.
없는 프레임이 있는 척하지 않는다. `img` 651개가 사라져 이미지 디코딩도 그만큼 준다.

### 남은 중복

안 부른 곡의 행이 같은 말을 세 번 한다 —
`이력 없음` · `아직 부르지 않음` · `노래책 수록`(950행 전부에 참인 사실).
문법상 오른쪽 칸은 Song 집계 자리이고 안 부른 곡은 집계가 없으므로 비는 것이 맞다.
다음 작업으로 남긴다.

### 확인

- 부른 곡 썸네일과 안 부른 곡 자리 모두 144×81, 행 높이 동일.
- `.song-list-row.unsung img` 0개.
- 가로 스크롤 0.


## 2026-08-21 같은 방송에서 두 번 부른 곡의 순번 뒤집힘 (1.0.8)

1.0.7 에서 넣은 통산 순번이 **한 방송에서 같은 곡을 두 번 부른 경우 뒤집혀** 나왔다.

```
2026-08-18  여름 축제 06:50:48 → 2번째   (먼저 부른 쪽)
            여름 축제 06:55:46 → 처음     (나중에 부른 쪽)
```

원인은 `takes` 배열의 정렬을 확인하지 않고 `takes.length - index` 로 센 것이다.
실제 배열은 **날짜는 최신순인데 같은 날 안에서는 시각순**이라 순서가 섞여 있다.

```
[0] 2026-08-18 06:50:48
[1] 2026-08-18 06:55:46      ← 날짜가 같으면 오래된 것이 먼저 온다
```

`(날짜, 시작초)` 로 직접 정렬한 뒤 자리를 찾도록 고치고, 계산을 `takeOrdinal()`
한 곳에 모았다. 다른 자리에서 같은 순번을 쓸 때 규칙이 갈라지지 않게 한다.

`DATA_MODEL.md` 가 이미 경고하고 있던 함정이다 —
"같은 방송에서 같은 곡을 여러 번 부를 수 있으므로 `(song_id, date)` 를 가창 식별자로
사용하지 않는다". 식별자로 쓰지는 않았지만 **정렬을 날짜만으로 가정한 것**이 같은 실수였다.

`findTake` 는 `perf_id` 를 먼저 보므로 두 무대를 구분하는 것 자체는 정상이었다.
검증에서 "서로 다른 값이 나온다"까지만 보고 **순서가 맞는지를 보지 않은 것**이 놓친 지점이다.

### 확인

- `06:50:48 → 처음`, `06:55:46 → 2번째`.
- 호시아이 `8번째` = 곡 페이지 `8개 방송 · 8번` 그대로.
- 빈 슬롯 0/22.


## 2026-08-21 색은 걷어내고 정보는 늘리기 (1.0.7)

정보량은 부족한데 난잡해지면 안 된다는 요구였다.
요소를 더하는 대신 **이미 있는 요소가 더 많은 뜻을 지도록** 했다.

### 장르색 도배 걷어내기

세어 보니 갈래가 극단적으로 갈려 있었다.

```
song.html   장르색 요소 955개  (950행 전부 + 거르개 칩 5개)
main.html   장르색 요소   4개  (비율 바 구간)
```

노래책 행은 `● 보컬로이드 GUMI` 처럼 **점과 이름이 20px 안에서 같은 말을 두 번**
하고 있었다. 점을 지워도 잃는 정보가 0인데 950개가 화면을 물들이고 있었다.

색이 제값을 하는 곳은 **서로 견주는 자리**다 — 비율 바, 통계 표, 가수 막대, 매트릭스.
순서대로 훑는 목록에서는 950개 점을 비교할 일이 없으므로 색이 정보가 아니라 소음이 된다.
게다가 이 사이트는 장르를 필요할 때 부르는 방법을 이미 갖고 있다(거르개 칩, 비율 바 hover).

지운 곳: 노래책 행 950 · 새로움 카드 14 · 곡 상세 1.
남긴 곳: 거르개 칩 5개(색 코드를 정의하는 범례), 그리고 비교가 일어나는 도형 전부.
`955개 → 5개`.

### 그 과정에서 나온 버그

새로움 카드의 점 14개는 **`0×0` 으로 그려지고 있었다.** `.genre-dot` 의 크기 규칙이
`book.css` 에 있는데 `main` 은 그 모듈을 싣지 않는다 — 월간 탐색에서 찾았던 것과
같은 의존성 버그다. 살리지 않고 마크업째 걷어냈다.

### 빈칸에 정보 넣기

셋리스트의 `처음` 배지 자리는 **22행 중 7행에서 빈칸**이었다.
`takes` 에서 이 무대의 자리를 찾아 통산 몇 번째인지 센다.
(1.0.7 에서는 "최신순이므로 뒤에서 센다"고 적었는데 **틀렸다** — 1.0.8 에서 바로잡았다.)

- `처음`(통산 1번째)은 그대로 눈에 띄게 — `--mark`
- 그 외는 `8번째` 처럼 조용하게 — `--faint`

같은 축의 값이라 자리도 같고, 새 요소도 새 색도 늘지 않는다. 빈칸 `7/22 → 0/22`.

### 철회한 제안

- **셋리스트 2열** — 시간순으로 훑는 목록을 두 갈래로 쪼개면 시선 흐름이 깨진다.
- **시각 단추 테두리에 장르색** — 22행에 색을 더 뿌리는 셈이고, 그 정보는 바에 스치면
  이미 나온다. 밀도를 올리려다 도배가 될 뻔했다.

### 확인

- 노래책 장르색 요소 955 → 5. 곡 줄은 `보컬로이드` 글자만 남는다.
- 셋리스트 빈 슬롯 0/22, 잘린 제목 5/22 로 변화 없음(회귀 아님).
- 통산 횟수 검산: 호시아이 `8번째` = 곡 페이지의 `8개 방송 · 8번`.
- 가로 스크롤 0.


## 2026-08-21 '가창' 걷어내기 (1.0.6)

`가창`이 낡은 말이라 `노래`로 바꿨다. 화면에 나가는 12자리를 손봤다.

- `실제 가창` → `실제 노래` (통계 비율 바, 표 머리)
- `가창 커버리지` → `노래 커버리지`
- `최장 연속 가창` → `최장 연속 노래`
- `노래책 보유 대비 실제 가창 장르` → `… 실제 노래 장르`
- 월간 요약 `가창 321번` → `노래 321번`
- 장르 tooltip `영어 가창: 100회` → `영어 노래: 100회`
- 상태 거르개 `aria-label="가창 상태 필터"` → `"노래 상태 필터"`

### 기계적으로 바꿀 수 없던 자리

`미가창`은 `미노래`가 되지 않는다. 그래서 이 줄만은 판단이 필요했는데,
**바로 아래 필터 칩이 이미 자연스러운 말을 쓰고 있었다.**

```
전   뱃지  노래책 950 · 가창 이력 299 · 다시 부름 103 · 미가창 651
     칩    전체 950 · 부른 곡 299 · 다시 부른 곡 103 · 아직 안 부른 곡 651
후   뱃지  노래책 950 · 부른 곡 299 · 다시 부른 곡 103 · 안 부른 곡 651
```

같은 네 숫자를 한 화면에서 두 가지 말로 부르고 있었던 셈이다.
뱃지를 칩 쪽 어휘에 맞춰 `노래 이력` 같은 새 말을 만들지 않았다.

### 단위도 함께 맞췄다

같은 수치를 `archive.page.html`은 `가창 488건`, `recap.js`는 `가창 321번`으로
세고 있었다. 둘 다 `번`으로 통일했다.

### 확인

- 소스와 빌드 산출물(데이터 포함) 모두에서 `가창` 0건.
- 5개 페이지에서 화면 텍스트·`title`·`aria-label`·`alt` 누수 0, 가로 스크롤 0.


## 2026-08-21 통계 비율 바에 분류명 (1.0.5)

통계의 `노래책 보유` · `실제 가창` 두 비율 바가 색과 퍼센트만 보여 주고 있었다.
색이 무엇을 뜻하는지 그 자리에서 말하지 않으면 표까지 내려가 대조해야 한다.
최근 방송 바와 같은 규칙으로 맞췄다.

- `>= 12%` → `보컬로이드 52%` (이름 + 비율)
- `>= 5%`  → `52%` (비율만)
- 그 아래  → 비우고 tooltip 에 맡긴다

라벨은 `.gc-bar-seg-label` 로 감싸 좁은 칸에서 말줄임으로 잘리게 했다.
정렬을 위해 `.gc-bar-seg` 에 `min-width: 0` 을 줬다 — 없으면 flex 자식이 안 줄어든다.

### 이 작업에서 낸 회귀와 원인

`genreLabel` 의 폴백을 `GENRE_LABEL["OTHERS/기타"]` 로 뒀더니 표에 없는 값이 전부
`기타` 로 접혔다. 통계에는 `OTHERS/기타`(분류된 범주)와 `미분류`(아직 분류 안 됨)가
**둘 다 행으로 있는데 한 이름으로 합쳐져** `기타` 가 두 줄로 나왔다.

폴백을 **키를 그대로 돌려주는 것**으로 바꿨다. 호출부 대부분은 이미
`GENRE_ORDER.includes(g) ? g : "OTHERS/기타"` 로 걸러 넘기므로, 이 폴백은
통계처럼 원본 값을 그대로 그리는 자리에서만 쓰인다.

### 검사가 놓치고 있던 누수

앞선 어휘 통일에서 "렌더링에 DB 키 없음"을 확인했는데, 그 검사가
**자식 요소가 있는 노드를 통째로 건너뛰고** 있었다.
`item.append(dot, document.createTextNode(...))` 처럼 아이콘과 글자를 함께 담은
요소가 정확히 그 모양이라, 월간 화면의 장르 범례에 `ENGLISH/영어 20%` 가
그대로 나오고 있는데도 통과로 나왔다.

검사를 **직계 텍스트 노드**를 보도록 고치고, `title` · `aria-label` · `alt` 도 함께 훑었다.
그래서 세 곳을 더 찾았다.

- `recap.js` 월간 장르 범례 — 화면에 보이는 글자
- `recap.js` 월간 바 tooltip
- `mix.js` 장르 칸 `aria-label` — 화면에는 없지만 **스크린 리더가 `ENGLISH/영어` 라고 읽고 있었다**

### 손대지 않은 것

월간 화면의 `month-bar` 는 높이 8px 짜리 얇은 요약 지표라 글자가 들어갈 자리가 없다
(최근 방송 바는 28px). 여기에 이름을 넣으려면 높이를 세 배로 키워야 하므로
요청 범위 밖으로 두고 tooltip 표기만 고쳤다.

### 확인

- 5개 페이지에서 화면 텍스트 누수 0, `title`/`aria-label`/`alt` 누수 0, 가로 스크롤 0.
- 통계 표의 장르 행: `보컬로이드 / J-POP·애니 / 영어 / 한국어 / 기타 / 미분류` — 중복 없음.
- 비율 바 라벨 잘림 없음.


## 2026-08-21 배포 1.0.4

`11qaws/rekatime-archive` 에 `e565b8f` 로 배포했다. 이번 묶음은 상태·상호작용 정의,
크기 체계 정규화, 장르 어휘 통일, 상호작용 어휘 3종 네 가지다.

### 배포에서 조심한 것

- **`hls.json` 은 건드리지 않았다.** 워크스페이스 사본이 배포본보다 오래돼서
  그대로 복사했으면 서명이 만료된 옛 주소로 되돌릴 뻔했다.
  이 파일은 12시간마다 도는 자동 갱신 작업의 소유다.
- 푸시가 한 번 거부됐다. 원격에 `chore: HLS 주소 갱신` 커밋이 먼저 올라와 있었다.
  건드린 파일이 `hls.json` 하나뿐이라 겹치지 않아 리베이스로 통합했다.
  **강제 푸시는 쓰지 않는다** — 자동 갱신 커밋을 지우게 된다.
- `index.html` 과 `main.html` 은 같은 `--pages` 변형이어야 한다(내비의 홈 링크가 `./`).
  `build_pages.py --pages` 는 main 을 `index.html` 로만 쓰므로 `main.html` 에도 같은 파일을 복사한다.
  이 규칙을 모르면 `main.html` 이 이전 빌드로 남는다 — 검증 중에 실제로 한 번 겪었다.
- `.wrangler/` 는 추적하지 않는다. 파일을 명시해서 add 했다.

### 배포 후 확인

- Pages 빌드까지 한 번 더 기다려야 했다(첫 요청은 옛 코드였다).
- 라이브에서 `VERSION=1.0.4`, 장르 바 고정 해제 단추, 대표 장면 ▶, 곡 카드 고르기,
  셋리스트 흐림, 장르 표기(`보컬로이드`) 확인.
- 가수 점선 밑줄은 `book.css` 를 싣는 `song.html` 에만 있다. 처음에 `index.html` 에서
  찾다가 없다고 판단할 뻔했다 — 페이지마다 싣는 모듈이 다르다는 걸 검증에도 반영해야 한다.


## 2026-08-21 장르 어휘 통일과 상호작용 어휘 3종

### 장르 어휘 — 키와 표기를 나눈다

같은 분류를 화면마다 다르게 부르고 있었다.
최근 방송은 `보컬로이드 / J-POP·애니 / 영어`, 노래 화면은 `VOCALOID / J-POP/애니 / ENGLISH/영어`.
뒤쪽은 사람이 읽을 말이 아니라 **DB 키가 그대로 새어 나온 것**이었다.

원인은 정의가 세 군데로 갈라져 있던 것이다 — `core.js`가 색, `mix.js`가 순서와 표기,
`stats.template.html`이 자기 사본. 그래서 표기를 거치는 화면과 안 거치는 화면이 생겼다.

- `GENRE_ORDER` · `GENRE_COLOR` · `GENRE_LABEL`을 **`core.js` 한 곳**으로 모으고
  `genreLabel(g)`를 통해서만 화면에 그린다. `mix.js`의 사본은 지웠다.
- 키(`VOCALOID`)는 **식별자로 그대로 둔다.** 주소(`?genre=ENGLISH/영어`)와 비교에 쓰이므로
  바꾸면 통계에서 노래책으로 넘어오는 기존 링크가 깨진다.
  `?genre=ENGLISH/영어`로 들어와도 `영어 146곡`으로 표시되는 것을 확인했다.
- 통계는 `core.js`를 싣지 않아 표를 따로 갖는다. 주석으로 함께 고치도록 못 박았다.

고친 자리: 노래책 거르개 칩, 곡 줄, 곡 상세, 통계 장르 표.

### 상호작용 어휘 3종

무엇을 하는 물건인지 모양으로 구분되지 않았다. 규칙을 셋으로 정하고 맞췄다.

```
테두리 단추      = 실행한다      재생 · 소리만 듣기
실선 밑줄(hover) = 이동한다      곡 상세 · 방송 상세
점선 밑줄        = 걸러낸다      가수 (화면을 떠나지 않는다)
```

- **대표 장면에 ▶ 를 얹었다.** 이 화면의 주 재생 버튼인데 기호가 하나도 없고
  `섬네일을 눌러 재생`이라는 글자뿐이었다. 얼굴 한가운데를 덮는 자리라
  평소 `opacity .8`로 물러나 있다가 겨눌 때 또렷해진다.
  기호가 생겼으므로 캡션의 중복 설명은 뺐다(미리보기 문구는 기호가 없으니 남긴다).
- **시각과 소리만 듣기에 테두리**를 줘 형제로 보이게 했다. 둘 다 '실행'이다.
  원본이 없어 누를 수 없는 곡의 시각은 `span`이라 테두리를 주지 않는다 — 단추가 아니다.
- **가수 950개에 점선 밑줄.** 누르면 목록이 걸러지는데 아무 표시가 없었다.
  처음에 `--line`으로 그었더니 배경과 거의 같은 색이라 보이지 않아 `--faint`로 올렸다.
- `처음` 배지의 테두리 알약은 앞선 작업에서 이미 없앴다 —
  이 어휘에서 테두리 알약은 '실행'이므로 상태에 쓰면 눌리는 것처럼 보인다.

곡 제목(`listrow-link` 22개, `song-list-link` 299개)은 **쉴 때 밑줄이 없고 hover 에만 긋는다.**
목록에서 제목마다 상시 밑줄을 치면 소음이 되고, 목록의 제목은 링크로 읽히는 것이 관습이다.

### 시각 단추가 자리를 더 먹는 문제

테두리와 여백이 붙으면서 시각 칸이 `64 → 72px`이 필요해졌다.
바로 앞 작업에서 제목 폭을 확보하려고 줄인 값이라, 길이 칸을 `40 → 36px`로 줄여 되찾았다.
잘린 제목은 1440에서 5/22로 유지된다.

### 확인

- 렌더링된 텍스트에 DB 키 없음(`<style>`·`<script>` 안의 데이터만 매칭).
- `?genre=ENGLISH/영어` 구 주소 정상 동작.
- 세 어휘가 실제 계산된 스타일에서 구분됨:
  실행 `border 1px + hover border-color:--mark` / 이동 `hover underline solid` /
  걸러내기 `underline dotted --faint`.
- 가로 스크롤 0. `ui_smoke_check.py` 통과.


## 2026-08-21 크기 체계 정규화 (글자 2의 배수 · 그 외 4의 배수)

글자 크기는 **2의 배수 정수**, 그 밖의 크기는 **4의 배수**로 맞췄다. 총 381건을 바꿨다.

- 글자 사다리: `30 / 24 / 20 / 18 / 16 / 14 / 12 / 10` 여덟 단.
  이전에는 13종이었고 `10 / 10.5 / 11 / 11.5`처럼 눈으로 구분되지 않는 값이 네 단을 차지했다.
  주력이던 `13px`(63곳)은 14로, `11.5px`(47곳)은 12로, 본문 기준 `15.5px`는 16으로 올렸다.
- 간격·크기: `4 / 8 / 12 / 16 / 20 / 24 / 28 / 32 / 36 …`.
  가장 많이 쓰이던 `14px`(58곳)은 16으로 올렸다.
- radius 토큰도 `3 / 6 / 10` → `4 / 8 / 12`. 알약(999px)까지 합쳐 네 종이다.

### 무엇을 건드리지 않았는지

`크기`를 글자·간격·상자로 한정하고 아래는 그대로 두었다. 선과 광학 디테일은
4의 배수로 만들면 오히려 어긋난다.

- `border-width` · `outline-width` · `outline-offset` — 1px, 2px 그대로
- `box-shadow`, `text-underline-offset`
- `@media` 분기점(1079 / 861 / 768 / 620 / 420) — `n-1` 경계값이다
- `--radius-pill: 999px` — 크기가 아니라 '무한' 표시다

### 변환기에서 한 번 틀린 것

처음 만든 변환기는 줄의 **첫 속성만** 보고 판단했다. 이 저장소는
`.x { display: flex; gap: 14px; border: 1px solid var(--line); }`처럼 한 줄에 선언을
여러 개 쓰므로, `padding`으로 시작하는 줄 뒤에 붙은 `border: 1px`까지 4px로 바꿔
**테두리를 네 배로 굵게** 만들었다. 백업에서 되돌리고 줄 전체에서 `속성: 값`을
하나씩 찾아 각각 판단하도록 고쳤다. 선택자와 선언이 한 줄에 있는 규칙
(`.sec-why { color: …; font-size: 13px; }`)도 이 방식이라야 잡힌다.
검사는 `1px solid` 개수가 변환 전후로 같은지로 했다.

### 글자가 커지며 생긴 회귀와 처리

`1440`에서도 셋리스트는 **393px**뿐이다 — 사이드바가 240px을 가져가기 때문이다.
그 폭에 시각·제목·가수·상태·길이·듣기 여섯 개가 들어가므로 글자가 커지자 제목이 먼저 잘렸다.
`1024`에서는 22곡 중 17곡이 `Hen…` `FRI…`처럼 알아볼 수 없게 됐다.

- 시각 칸 `72px → 64px` — 실측 필요 폭이 58px이라 14px을 놀리고 있었다.
- 셋리스트 최소 폭 `332px → 360px`.
- `1079px` 이하에서는 셋리스트에서 가수를 내렸다. 이 줄의 주인공은 제목이고,
  가수는 새로움 카드와 곡 화면에 그대로 있다.
- 가수 최대 폭 `38% → 28%`.
- `처음` 배지의 테두리 알약을 없앴다. 이 화면에서 테두리 알약은 '실행'을 뜻하는데
  `처음`은 상태여서 눌리는 것처럼 보였다. 글자만 남기니 35px → 18px 이 됐다.

결과: 1024에서 잘린 제목 17/22 → 4/22, 1440에서 6/22 → 5/22.
남은 것은 `카사네 테토는 이런 파티를…`처럼 어느 폭에서도 잘릴 긴 제목이다.

### 확인

- main / archive / song / broadcast / stats 다섯 페이지 × 1440·1024 폭에서
  가로 스크롤 0, 부모 밖으로 새는 요소 0.
- 390px 은 헤드리스 Chrome 의 최소 창 폭(500px)에 걸려 별도로 내장 브라우저에서 확인했다.
- 밝은 테마 확인. `ui_smoke_check.py` 통과.
- 통계의 `.bp-point` 22개가 16px 미만이지만 이는 차트의 데이터 점이고
  정규화로 13px → 12px 이 된 것뿐이다.


## 2026-08-21 상태·상호작용 정의 공백 메우기

화면에는 있는데 **눌렀을 때·스쳤을 때 무엇이 변하는지 정의되지 않은 지점**을 훑어 메웠다.
JS가 만드는 상태와 CSS가 그리는 상태를 대조하는 방식으로 찾았다.

- **장르 바 hover가 셋리스트에 닿지 않았다.** `day.js`는 진작부터 행에 `data-dim`을 붙이고
  있었는데 `.listrow[data-dim]`을 그리는 규칙이 없어 띠 안쪽만 흐려지고 곡 목록은 무반응이었다.
  `.day-rows .listrow[data-dim="true"] { opacity: .26 }`을 넣고, 같은 규칙을 새로움 카드에도 이었다.
- **장르 바는 눌러도 아무 일이 없었다.** `<button type="button">`에 `cursor:pointer`까지 갖췄지만
  `mix.js`에 click 리스너 자체가 없었다. 어포던스만 있고 동작이 없는 상태였다.
  스침=미리보기, 누름=고정으로 나누고 `aria-pressed`와 안쪽 테두리로 두 상태를 구분했다.
  고정 중에는 다른 칸에 스쳐도 풀리지 않는다(고정이 미리보기를 이긴다).
- **처음·오랜만 카드가 화면 밖으로만 나갔다.** 카드를 누르면 곡 상세로 이탈할 뿐,
  같은 화면의 셋리스트·대표 장면과 연결되지 않았다. 카드를 `고르기`(덮개 버튼)와
  `기록 →`(링크) 둘로 나눠, 카드를 누르면 해당 행이 선택되고 대표 섬네일이 그 곡으로 옮겨간다.
  행이 목록 밖이면 굴려서 보여 주고 900ms 동안 한 번 번쩍여 어디로 갔는지 알린다.
  중첩 클릭 대상을 만들지 않으려고 대표 장면(`.day-cover`)과 같은 덮개 버튼 방식을 썼다.
- **되돌아가는 길을 추가했다.** 셋리스트 행에 스치면 그 곡이 띠의 어느 색인지 밝아진다.
  고정된 상태는 건드리지 않는다.

### 스타일 없이 나오던 월간 탐색

`recap.js`의 월간 이전/다음은 day view의 `.day-nav`/`.day-step`을 빌려 쓰는데
**`archive` 페이지의 CSS 목록에 `day`가 없었다.** 그래서 `← 지난달2026년 7월 · 방송 4회`처럼
라벨과 설명이 한 줄로 붙고 색도 브라우저 기본 링크색으로 나왔다.
`build_pages.py`의 `archive` CSS 목록에 `day`를 넣어 의존을 명시하고,
정의가 없던 `.day-step-l`·`.day-step-d`·`.right`를 채웠다.
`.day-nav`는 달력 칸(260px)을 전제한 3칸 격자여서 달력 없는 월간 탐색에서는
`다음 달`이 가운데 칸에 들어갔다 — `without-calendar`를 붙여 2칸으로 맞췄다.

### 화면으로 확인하고 나서 더 고친 것

DOM 계측만으로는 통과였는데 실제 렌더링을 보니 두 가지가 더 나왔다.
(내장 미리보기 창과 Chrome 확장이 모두 막혀 있어, 설치된 Chrome을 헤드리스로 돌려
계측본을 캡처했다. 전이가 끝난 모습을 잡으려고 계측본에서는 transition/animation을 껐다.)

- **스침과 고정이 화면으로 구분되지 않았다.** 잠금 테두리를 `--on-color`(어두운 색)로
  넣었는데 장르색 띠 위에서는 보이지 않아, hover 화면과 click 화면이 사실상 같았다.
  밝은 테두리(`rgba(255,255,255,.92)`)로 바꾸고, **무엇만 보고 있는지와 되돌리는 길**을
  띠 아래 한 줄로 밝혔다 — `영어 11곡만 보는 중  [× 전체 보기]`.
  상태를 색으로만 말하지 않고 글과 되돌리기 단추로 함께 말한다.
- **`.thumb-badge`에 CSS가 아예 없었다.** 앵커 안의 배지라서 브라우저 기본 링크색(보라)에
  밑줄이 그어진 채로 월간 화면과 곡 화면에 나오고 있었다.
  `--scrim` 위 `--on-media` 배지로 정의하고, 앵커의 기본 링크 스타일도 `.thumb-wrap`에서 끊었다.

앞선 감사는 JS가 **바꾸는 상태**(`classList`/`setAttribute`)만 봤다.
`el()`로 **만드는 클래스** 중 CSS가 없는 것도 같은 종류의 공백이라 따로 훑었다 —
174종 중 3종이 비어 있었다(`.thumb-badge`, `.mix-note-t`, `.youtube-player`).
`.youtube-player`는 YouTube API가 iframe으로 갈아치우는 자리표시자라 그대로 둔다.

흐림은 `opacity`만으로는 색이 살아 있어 장르색·상태색이 그대로 읽혔다.
`filter: saturate(.2)`를 더해 물러난 곡은 색도 함께 빠지게 했다.

### 확인

- 1440×900 / 1024×768 / 390×844에서 가로 스크롤 없음(`scrollLeft`가 실제로 움직이지 않음).
  측정 시 나오던 4px은 세로 스크롤바 폭이지 넘침이 아니다.
- `기록 →` 링크를 절대 배치로 두면 가수 이름을 덮어서, 아래 한 줄(`.news-foot`)을 만들어
  가수는 왼쪽, 링크는 오른쪽으로 흐름 안에 두었다. 세 해상도에서 겹침 0.
- 모바일에서 카드 고르기 버튼은 전부 44px 이상.
- `ui_smoke_check.py` 통과.

> 검증 메모: 창이 표시되지 않은 상태(`document.visibilityState === "hidden"`)에서는
> CSS 전이가 진행되지 않아 `getComputedStyle`이 전이 **시작값**을 돌려준다.
> 값 자체를 확인할 때는 `transition: none`으로 분리해서 재야 한다.


## 2026-08-20 RekaPlayer 소스 우선순위 교정

- 재생 소스 선택 순서를 `페어링된 YouTube → CHZZK HLS → 섬네일과 안내`로 교정했다.
- `embed_kind=youtube`를 페어링의 단일 판별 기준으로 사용하고 YouTube 링크와 공식 embed를 먼저 연다.
- 원본이 삭제됐더라도 YouTube 페어링이 있는 곡은 재생할 수 있고, 둘 다 없으면 링크 없는 섬네일과 기록 안내만 표시한다.

## 2026-08-20 RekaPlayer 영상 연결

- CHZZK 페이지 전체를 iframe으로 불러 CSS 좌표로 잘라내던 crop 구조를 제거했다.
- 기존 `hls.json` 갱신 데이터와 hls.js를 재사용해 CHZZK 방송은 실제 `<video controls>`에 signed HLS를 연결한다.
- 방송 날짜와 `session_id`로 원본 플랫폼을 판별하므로, 가창 링크가 YouTube여도 CHZZK 방송 원본이 살아 있으면 HLS를 우선한다.
- HLS가 없거나 만료·재생 불가이면 YouTube 공식 embed 또는 원본 사이트 링크로 단계적으로 내려간다.
- CHZZK 페이지 주소는 iframe fallback으로 허용하지 않아 crop 제거 후 전체 CHZZK 페이지가 다시 나타나는 회귀를 막았다.
- 플랫폼별 구현은 `play`, `pause`, `seek`, `destroy` 네 동작만 공통으로 두어 셋리스트 시각 클릭이 같은 경로를 사용한다.

## 2026-08-20 최근 방송 제목 상단 여백 통일

- 최근 방송에만 적용되던 데스크톱 20px, 태블릿 10px 상단 여백 축소를 제거했다.
- 공통 `main` 여백 36px을 그대로 사용해 월간 정리와 제목 시작 위치의 호흡을 맞췄다.

## 2026-08-20 하단 곡 목록의 카드 장식 제거

- 생성형 대시보드처럼 보이던 곡별 배경, 라운드, 색상 테두리와 섹션 장식선을 제거했다.
- 곡 목록은 페이지 바탕 위의 평평한 열로 두고, 얇은 기준선과 24px 열 간격만으로 구분한다.
- 장르색은 작은 점에만 남겨 제목과 가수 정보보다 강하게 보이지 않게 했다.

## 2026-08-20 최근 방송 여백과 강조 밀도 조정

- 대표 장면과 셋리스트 사이 간격, 하단 분류 사이 간격을 늘려 큰 정보 묶음이 서로 붙어 보이지 않게 했다.
- 곡 목록의 통짜 격자판을 없애고 실제 배경 여백이 보이는 독립 카드로 바꾸되, 장르색은 2px 보조선에만 남겼다.
- 최근 방송의 첫 등장 곡은 최대 10곡까지만 우선 노출하고 반복되던 `첫 등장` 상태 문구를 제거했다.
- 장르 비율 바 높이를 30px에서 26px로 줄여 대표 장면보다 먼저 튀지 않게 했다.

## 2026-08-20 곡 카드 시각 소음 정리

- 곡마다 둘렀던 개별 테두리와 라운드를 제거하고 분류별 하나의 판 안에 평평한 셀로 정렬했다.
- 장르색은 카드 전체가 아니라 왼쪽 3px 강조선에만 사용해 제목 중심으로 읽히게 했다.
- 개별 방송 화면은 셋리스트와 중복되는 곡 카드를 분류별 8곡으로 제한하고, 최근 방송 화면은 전체 표시를 유지했다.

## 2026-08-20 최근 방송 하단 곡 카드화

- `처음 부른 곡`과 `오랜만에 부른 곡`을 좌우 요약에서 위아래 독립 섹션으로 바꿨다.
- 모든 해당 곡을 장르·상태·제목·가수가 있는 세로형 곡 카드로 표시해 잘림과 작은 글자 문제를 줄였다.
- 분류 섹션에는 별도 배경 카드를 두지 않고 곡 카드만 surface를 사용해 중첩 프레임을 피했다.

## 2026-08-20 최근 방송 달력 제거와 제목 양식 통일

- 최근 방송 화면에서는 달력을 생성하지 않고 이전·이후 방송 탐색만 남겼다.
- 최근 방송 제목 영역을 월간 정리와 동일한 `키커 → 제목 → 설명` 구조와 공통 `page-intro` 스타일로 통일했다.
- 개별 방송 날짜와 방송 수치는 제목 영역 아래의 한 줄로 분리했다.

## 2026-08-20 최근 방송 크기 위계 보정

- `최근 방송` 인트로와 날짜 방송 제목을 첨부 기준 화면처럼 왼쪽 한 흐름으로 복원했다.
- 상시 달력은 오른쪽 상단의 독립 탐색 열로 옮기고 이전·이후 버튼을 바로 아래에 묶었다.
- 상시 달력의 폭과 날짜 셀 높이를 줄여 상단 탐색 영역이 화면을 과도하게 차지하지 않게 했다.
- 방송 제목, 대표 장면 캡션, 셋리스트와 새로움 목록의 글자 크기를 한 단계 올려 읽기 위계를 분명하게 했다.
- 확보한 상단 높이를 대표 장면과 셋리스트에 돌려 기준 데스크톱에서 주 프레임 높이를 최대 440px로 키웠다.

## 2026-08-20 최근 방송 탐색 방향과 상시 달력 보정

- 장르명과 비율을 색 막대 안으로 옮기고 12% 미만의 좁은 구간은 텍스트만 생략해 형태가 깨지지 않게 했다.
- 방송 달력을 접는 메뉴에서 꺼내 제목 우측 탐색 영역에 항상 표시한다.
- 시간축을 최신 방송이 왼쪽, 이전 방송이 오른쪽이 되도록 버튼 순서와 키보드 탐색 방향을 통일했다.
- 데스크톱 콘텐츠 폭을 1380px까지 넓히고 처음 부른 곡과 오랜만에 부른 곡을 카드마다 최대 8곡까지 표시한다.
- 대표 장면 캡션의 위치 기준을 이미지 카드로 고정해 하단 곡 목록을 덮지 않게 했다.

## 2026-08-20 최근 방송 단일 화면 프레임 보정

- 대표 장면을 약 65%, 셋리스트를 약 35%로 재배분하고 두 영역의 높이를 동일하게 고정했다.
- 장르 비율바 내부에 보컬로이드·J-POP/애니·영어·한국어·기타의 비율과 곡 수를 상시 표시한다.
- 이전·달력·이후 탐색을 방송 제목 우측의 한 묶음으로 합치고 달력은 필요할 때 펼치는 방식으로 바꿨다.
- 처음 등장·오랜만 곡은 주 프레임 아래의 컴팩트 요약으로 축소해 기준 데스크톱 화면 안에 핵심 정보를 맞췄다.

## 2026-08-20 이전 템플릿 진입 경로 복구

- 더 이상 생성본이 아닌 `templates/main.html`을 열면 빈 화면이 되던 문제를 확인했다.
- 옛 경로를 `../main.html` 생성본으로 즉시 이동하는 호환 진입점으로 복구했다.

## 2026-08-20 최근 방송 프레임 재정돈

- 최근 방송을 페이지 이동 없이 좌우 스와이프·방향키·이전/다음 버튼으로 전환하도록 바꿨다.
- 대표 섬네일과 전체 셋리스트를 주 2열 프레임으로 복원하고, 긴 셋리스트는 그 프레임 안에서 스크롤한다.
- 달력은 주 2열에서 제거해 방송 맥락 카드와 함께 아래 보조 프레임으로 이동했다.
- 860px 이하에서는 섬네일, 셋리스트, 달력이 한 열로 이어지도록 유지했다.

## 2026-08-20 노래 카드 횟수별 묶음 제거

- `n개 방송에서`처럼 가창 횟수마다 목록을 끊던 제목과 정렬 축을 제거했다.
- 검색·상태·장르·태그 조건을 통과한 곡은 하나의 연속 카드 그리드에 바로 렌더링한다.
- 카드 내부의 방송 횟수는 개별 곡 정보로 유지하되 목록의 구조로 사용하지 않는다.

## 2026-08-20 노래 목록과 개별 기록 통합

- `song.html`의 기본 화면을 전체 노래 카탈로그로 바꾸고 기존 검색·상태·장르·태그·가수 필터를 함께 옮겼다.
- 950곡을 같은 크기의 카드로 표시하며, 가창 이력이 없는 곡도 `미가창` 상태의 점선 카드로 남긴다.
- 가창 이력이 있는 카드는 `song.html?id=<song_id>`의 개별 기록으로 이어지고, 뒤로 가기는 같은 페이지의 전체 목록으로 돌아온다.
- 기존 `book.html` 주소는 검색 조건을 보존한 채 `song.html`로 이동하는 호환 경로로 남겼다.

## 2026-08-20 최근 방송을 사이트 진입점으로 전환

- 방문자의 첫 의도를 `가장 최근 방송에서 무엇을 불렀는지 확인하고 재생하기`로 정의했다.
- `main.html`을 최신 방송 1회에 집중하는 진입점으로 바꾸고, 월간 탐색은 `archive.html`의 독립 탭으로 분리했다.
- 내비게이션을 `최근 방송 / 월간 정리 / 노래 / 통계` 네 축으로 재구성했다.
- 곡 상세도 `song.html?id=<song_id>`로 분리해 최근 방송과 월간 화면의 역할이 섞이지 않게 했다.
- 기존 `main.html?month=`, `?song=`, `?broadcast=`, `?date=` 주소는 새 전용 페이지로 넘긴다.

## 2026-08-20 월간 목록과 개별 방송 분리

- `main.html`은 월간 달력, 월간 방송 목록, 월간 통계에 집중하도록 재구성했다.
- `broadcast.html?id=<session_id>`를 추가해 대표 장면, 방송 맥락, 전체 셋리스트를 한 화면에 모았다.
- 중복되던 홈 히어로와 별도 날짜별 방송 목록을 제거했다.
- 기록 상태와 원본 상태를 분리했다. 2026-07-06만 시스템 구축 전 누락으로 표시하고, 2026-08-01은 완성된 방송 기록에 `원본 삭제 · 기록 보존` 상태를 표시한다.
- `archive_annotations.json`을 projection 입력으로 추가해 원본 데이터와 아카이브 상태를 섞지 않고 관리한다.

## 2026-08-19 — 조립 템플릿 직접 노출 수정

- CSS·JS가 들어가기 전의 `*.page.html`을 직접 열면 디자인이 깨지는 원인을 확인했다.
- 실제 템플릿을 `templates/`로 이동하고 루트의 이전 경로는 완성본으로 넘기는 호환 진입점으로 바꿨다.
- 사용자 진입점은 `main.html`, `book.html`, `stats.html` 세 파일로 고정했다.

## 2026-08-19 — 라이브 아카이브 구조 재편

- 페이지 목적을 `라이브 분석창 + 최종 방송 아카이브`로 정의했다.
- 홈의 흐름을 `지금의 장면 → 날짜별 방송 → 월간 흐름 → 누적 노래`로 재배치했다.
- 모든 방송 카드에 대표 썸네일, 셋리스트 미리보기, 월간 이동을 결합했다.
- 방송 상세에 대표 장면, 달력, 새로움, 전체 셋리스트를 한 흐름으로 배치했다.
- 방송 주소를 날짜에서 `session_id`로 전환하고 기존 `?date=` 주소는 호환했다.
- Rekatime 원본에서 main/book/stats와 썸네일을 함께 동기화하는 `sync_archive_data.py`를 추가했다.
- 참조 무결성, 집계 수, 동일 `source_revision`, signed HLS 미저장을 빌드 검사에 포함했다.
- 1440×900, 1024×768, 390×844에서 가로 넘침과 콘솔 오류가 없음을 확인했다.
# 2026-08-20 8월 4일 CHZZK 원본 삭제 반영

- 사용자 확인에 따라 `chzzk-14524675`(2026-08-04)를 `record_status=complete`, `source_status=removed`로 기록했다.
- 셋리스트와 섬네일은 보존하고, 플레이어에서는 죽은 원본 링크 대신 삭제 안내를 표시한다.
- 회귀 검사는 8월 1일과 8월 4일 두 삭제 방송을 함께 검증한다.
# 2026-08-20 YouTube 오류 153 방지

- 원인: `file://` 실행에서는 YouTube iframe 요청에 HTTP Referer가 없어 오류 153이 발생했다.
- 웹 실행에서는 `enablejsapi=1`과 함께 현재 웹 출처를 `origin`으로 전달하고 `strict-origin-when-cross-origin` 정책을 명시한다.
- `file://` 실행에서는 오류가 나는 iframe 대신 웹 실행 안내와 YouTube 바로가기를 표시한다.
# 2026-08-20 셋리스트 선택과 재생 역할 분리

- 셋리스트 행을 누르면 해당 곡이 선택 상태로 고정되고 큰 섬네일·제목·재생 시각이 갱신된다.
- 곡 제목은 기존처럼 곡 상세 페이지로 이동한다.
- 큰 섬네일을 누르면 선택된 곡의 페어링 우선순위에 따라 재생한다. 시각 버튼의 빠른 재생은 유지한다.
- 원본이 없는 곡은 선택과 섬네일 확인만 가능하고 섬네일 재생 버튼은 비활성화한다.
# 2026-08-20 CHZZK 외부 열기 시각 보존

- 문제: 플레이어의 `치지직에서 열기/보기`가 곡 URL보다 방송 기본 URL을 우선해 `currentTime` 쿼리를 잃었다.
- 수정: 외부 열기 버튼 두 곳 모두 가창 단위 URL을 우선하고, 없을 때만 방송 URL로 대체한다.
# 2026-08-20 통계 연속 기록 개편

- `몇 시간째에 무엇을 부르나` 시간대 분석과 시간대 KPI를 제거했다.
- 같은 곡을 한 방송에서 여러 번 불러도 1회로 세고, 방송 순서상 최대 연속 가창이 3회 이상인 곡만 표시한다.
- 연속된 방송의 장르색 점 사이를 같은 색 띠로 이어 Aseprite 타임라인처럼 하나의 연속 구간으로 읽히게 했다.
- 곡별 오른쪽 수치는 총 등장 횟수가 아니라 최대 연속 방송 횟수다.
# 2026-08-20 최근 방송 16:9 인라인 재생

- 최근 방송의 큰 섬네일을 고정 16:9 재생 면으로 바꿨다.
- 섬네일 클릭 시 모달 대신 같은 자리에서 유튜브 또는 치지직 HLS를 재생한다.
- 다른 곡 선택, 방송 전환, Escape 입력 시 플레이어를 정리하고 선택된 섬네일로 복원한다.
- 개별 방송 화면과 셋리스트 시각 버튼의 모달 재생은 유지한다.

# 2026-08-20 셋리스트 호버 미리보기와 재생 유도

- 셋리스트 행을 호버하면 해당 곡의 섬네일과 캡션을 임시 미리보기하고, 포인터가 빠지면 클릭으로 고정했던 곡으로 복원한다.
- 재생 중인 인라인 플레이어는 호버로 교체하거나 중단하지 않는다.
- 재생 가능한 큰 섬네일에는 호버·키보드 포커스 시 유레카 레몬색 글로우를 표시하고, 모션 감소 설정에서는 전환을 생략한다.

# 2026-08-20 YouTube 재생 오류 자동 폴백

- 단순 임베드 화면의 153만으로 WebView 문제라고 판단할 수 없어 공식 YouTube IFrame API의 `onError`로 다시 확인했다.
- 유레카의 최신·기존 페어링 영상은 오류 150(소유자가 임베드 재생을 허용하지 않음), 같은 WebView의 YouTube 공식 샘플 영상은 정상 재생됐다. 따라서 현재 인라인 실패 원인은 영상의 퍼가기 허용 설정이다.
- YouTube가 페어링된 곡은 CHZZK로 대체하지 않는다. 임베드 허용 영상은 인라인 YouTube를 유지하고, 오류 150/153 등으로 막힌 영상은 오류 코드와 동일 타임스탬프의 YouTube 직접 재생 링크를 표시한다.

# 2026-08-20 월간 방송에서 최근 방송 프레임으로 연결

- 월간 정리의 방송 썸네일, 날짜, `방송 하나에 집중하기`, 달력 선택을 `main.html?broadcast=<session_id>`로 통일했다.
- 최근 방송 화면은 `broadcast` 쿼리의 회차를 같은 프레임 안에서 바로 열며, 이전·이후 버튼과 스와이프·방향키 이동도 주소 기록에 반영한다.
- 브라우저 뒤로가기·앞으로가기로 선택 회차를 복원하고, 기존 `broadcast.html?id=` 주소는 호환용으로 유지한다.

# 2026-08-20 삭제 원본 상태 문구 정리

- 원본이 삭제됐지만 셋리스트와 섬네일이 남은 방송의 상태 배지를 `기록 보존`으로 통일했다.
- 재생을 시도했을 때의 안내 문구는 삭제 이유를 설명할 수 있도록 유지한다.
- 월간 카드의 이동 링크 문구는 `방송 상세보기`로 정리했다.

# 2026-08-20 7월 6일 기록 메모 정리

- 2026-07-06의 달력 및 월간 목록 메모를 `집계 시스템 구축 이전 방송으로, 방송이 있었다는 기록만 있습니다`로 교체했다.
- 방송 부재가 아니라 집계 데이터만 없는 날이라는 기존 상태 구분은 유지한다.

# 2026-08-20 데스크톱 내비게이션 폭 조정

- 좌측 내비게이션 폭을 216px에서 240px로 넓혀 본문과 화면 가장자리의 시각적 균형을 맞췄다.
- 1079px 이하의 모바일·태블릿 하단 탭 구조는 변경하지 않았다.

# 2026-08-20 통계 요약 문구 정리

- 통계 KPI의 `방송`을 `노래방`, `그중 노래한 시간`을 `노래한 시간`으로 바꿨다.
- `방송당 평균` 곡 수는 소수 첫째 자리 대신 가장 가까운 정수로 반올림한다.
# 2026-08-20 페이지 제목 양식 통일

- 통계 화면을 기준으로 공통 제목 키커를 13px, 제목을 30px, 제목 묶음 간격을 8px로 통일했다.
- 최근 방송·월간 정리·방송 상세·노래·노래 상세의 키커를 `rekatime-clips · 화면명` 형식으로 맞췄다.
- 노래 목록에도 키커를 추가해 모든 주요 화면이 같은 2단 제목 구조를 사용한다.

# 2026-08-20 러닝 타임 분포 통계

- 시간대별 곡 타임라인을 제거하고 전체 러닝 타임과 실제 노래 시간의 가로 박스 플롯으로 교체했다.
- 같은 0시간 기준 축에 최저·하위 25%·중앙값·상위 25%·최고와 IQR 1.5배 아웃라이어를 표시한다.
- 모든 방송을 날짜 점으로 유지하고 hover·키보드 focus로 날짜와 시간을 확인하며 클릭하면 최근 방송 프레임으로 이동한다.
- 외부 차트 라이브러리 없이 고정 DOM 구조와 데이터 기반 눈금 계산을 사용해 데이터 업데이트 후에도 형태가 유지되게 했다.

# 2026-08-21 브랜드명과 저작권 정보

- 공통 좌측·모바일 내비게이션 브랜드명을 `유레카`에서 `레카타임`으로 변경했다.
- 통계 페이지 최하단에 팬메이드 비공식 콘텐츠 및 2차 창작 가이드라인 준수 사실을 `저작권 정보` 문단으로 추가했다.
# 2026-08-21 노래 목록을 상세 축약형으로 개편

- 분류 필터의 상태·장르·태그 기준선을 왼쪽으로 통일했다.
- 카드 격자를 제거하고 `16:9 미니 섬네일 → 곡 메타데이터 → 가창 이력`의 단일 목록으로 교체했다.
- 부른 곡은 최신 장면과 상세 기록으로 연결하고, 미가창곡은 플레이스홀더와 명시적인 상태로 같은 목록에 유지한다.
- 긴 목록의 초기 부담을 줄이기 위해 섬네일에 네이티브 지연 로딩을 적용했다.
# 2026-08-21 라이트·다크 모드 전환

- 데스크톱과 모바일의 레카타임 이름 오른쪽에 해·달 전환 버튼을 추가했다.
- 공통 색상 토큰에 라이트 팔레트를 추가하고 통계 페이지의 독립 토큰도 같은 값으로 맞췄다.
- 선택 모드는 `localStorage`에 저장하며 문서 본문이 그려지기 전에 적용해 화면 번쩍임을 줄였다.
